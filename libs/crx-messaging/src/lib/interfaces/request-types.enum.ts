export enum CrxRequestType {
    loadSettings = 'loadSettings',
    saveSettings = 'saveSettings',

    // Walmart
    walmartATC = 'walmartATC',
    walmartLogin = 'walmartLogin',
    walmartSetShipAddress = 'walmartSetShipAddress',
}
